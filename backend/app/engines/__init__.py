"""Pure calculation engines (payoff, PnL, slippage, margin local-model).

These are deterministic and dependency-light so they can be unit-tested against
known values (TDD anchors per CLAUDE.md).
"""
